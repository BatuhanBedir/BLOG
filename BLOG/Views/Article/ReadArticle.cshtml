@model ArticleIndexVM
@{
    ViewData["Title"] = "Read Article";
}
@*<div class="container my-5">
    <h2 class="text-center">@Model.Title</h2>
    <p class="text-muted">Written by <a href="/UsersArticle/@Model.UserId" >@Model.Writer</a></p>
    <a href="/UsersArticle/@Model.UserId"><img src="@Model.Image" width="100" height="100" /></a>
    <p class="text-muted">Published on @Model.CreatedTime</p>
    <p class="text-muted">Average Reading Time: @Model.AvgReadingTime minutes</p>
    <hr>
    <p>@Model.Content</p>
</div>*@
@*<div class="container my-5 d-flex align-items-center">
    <div class="text-center">
        <h2>@Model.Title</h2>
        <p class="text-muted">Written by <a href="/UsersArticle/@Model.UserId">@Model.Writer</a></p>
        <p class="text-muted">Published on @Model.CreatedTime</p>
        <p class="text-muted">Average Reading Time: @Model.AvgReadingTime minutes</p>
        <hr>
        <p>@Model.Content</p>
    </div>
    <div class="ml-5">
        <a href="/UsersArticle/@Model.UserId">
            <img src="@Model.Image" width="100" height="100" />
        </a>
    </div>
</div>*@
<div class="container my-5">
    <div class="row">
        <div class="col-md-3">
            @*<a href="/UsersArticle/@Model.UserId">
                <img src="@Model.Image" width="150" height="150" />
            </a>*@
            <a href="@Url.Action("UserDetail","User",new{id=Model.UserId})">
                <img src="@Model.Image" width="150" height="150" />
            </a>
            @*<p class="text-muted">Written by <a href="/UsersArticle/@Model.UserId">@Model.Writer</a></p>*@
            <p class="text-muted">Written by <a href="@Url.Action("UserDetail","User",new{id=Model.UserId})">@Model.Writer</a></p>
            <p class="text-muted">Published on @Model.CreatedTime.ToString("D")</p>
            <p class="text-muted">Average Reading Time: @Model.AvgReadingTime minutes</p>
        </div>

        <h2 class="text-center">@Model.Title</h2>
        <p>@Model.Content</p>
    </div>
</div>

